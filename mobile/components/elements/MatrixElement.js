import React from 'react';

import { View, Text } from 'react-native';

const MatrixElement = props => {

    return (
        <View>
            <Text>MatrixElement still under construction...</Text>
        </View>
    );
};

export default MatrixElement;