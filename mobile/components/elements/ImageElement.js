import React from 'react';
import { View, Text } from 'react-native';

const ImageElement = props => {
    return (
        <View>
            <Text>ImageElement still under construction...</Text>
        </View>
    );
};

export default ImageElement;