import React from 'react';

import { View, Text } from 'react-native';

const RatingElement = props => {

    return (
        <View>
            <Text>RatingElement still under construction...</Text>
        </View>
    );
};

export default RatingElement;