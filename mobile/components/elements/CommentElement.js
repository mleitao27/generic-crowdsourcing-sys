import React from 'react';

import { View, Text } from 'react-native';

const CommentElement = props => {

    return (
        <View>
            <Text>CommentElement still under construction...</Text>
        </View>
    );
};

export default CommentElement;