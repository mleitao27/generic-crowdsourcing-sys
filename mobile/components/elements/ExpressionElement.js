import React from 'react';

import { View, Text } from 'react-native';

const ExpressionElement = props => {

    return (
        <View>
            <Text>ExpressionElement still under construction...</Text>
        </View>
    );
};

export default ExpressionElement;