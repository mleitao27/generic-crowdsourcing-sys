import React from 'react';

import { View, Text } from 'react-native';

const HtmlElement = props => {

    return (
        <View>
            <Text>HtmlElement still under construction...</Text>
        </View>
    );
};

export default HtmlElement;