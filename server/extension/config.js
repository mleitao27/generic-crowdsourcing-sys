// Set admin credentials
const admin = [
    {
        email: 'admin',
        password: 'admin'
    }
];

// Set cache timeout
const userTimeout = 10;

// Exports
exports.admin = admin;
exports.userTimeout = userTimeout;