// Set admin credentials
const admin = [
    {
        email: 'admin',
        password: 'admin'
    }
];

// Set cache timeout
const userTimeout = 3600;

// Exports
exports.admin = admin;
exports.userTimeout = userTimeout;